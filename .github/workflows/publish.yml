name: Build and Push

on:
  pull_request:
    branches: ["*"]
  push:
    branches: ["main"]
  merge_group:

env:
  GHCR_NAMESPACE: mohammadshehadeh/lms

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Login to GHCR
        run: echo ${{ secrets.ACCESS_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build Image
        run: docker build -t ghcr.io/$GHCR_NAMESPACE/nextjs:latest apps/nextjs

      - name: Push Image
        run: docker push ghcr.io/$GHCR_NAMESPACE/nextjs:latest
